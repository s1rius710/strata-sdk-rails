# This is defined by the consuming app -- should move there?

rules:
  - type: MinimumWagesRule
    params:
      threshold: 6000
      exclude: true
  - type: CustomRules
    params:
      rules:
        - field: :hours_worked
          rule: MinimumHoursRule 
          operator: ">="
          value: 500