source "https://rubygems.org"

# Specify your gem's dependencies in flex_sdk.gemspec.
gemspec

gem "puma"

# Use postgresql as the database for Active Record
gem "pg"

gem "propshaft"

group :development, :test do
  gem 'rspec-rails', '~> 7.0.0'
end
