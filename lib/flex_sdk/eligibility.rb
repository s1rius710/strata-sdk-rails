module FlexSdk
  class Eligibility

    # placeholder code to validate the engine is working
    def self.check(data)
      data.each { |key, value| puts "#{key}: #{value}" }
       "Yup"
    end
  end
end
  